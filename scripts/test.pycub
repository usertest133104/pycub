metodfile(event)

on player join:
    broadcast(player name + "testsetseest")
    broadcast(player name + "testsetseest")
    stop

changefile(event,test)

on player join:
    broadcast(player name)
    stop

